# MobileFlashCards

## How to use

- Install packages with `npm` or `yarn install`
- Run the project wih `npm` or `yarn start`

## Features

- Allows users to study flashcards
- Users can create new decks that can contain an unlimited number of cards
- If a deck has at least one card, the user can then start the quiz at the end of which they will receive a result